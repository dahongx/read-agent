# 使用说明

文献阅读 Agent — 上传论文 PDF，自动生成可用于汇报的 PPT，并与 AI 就论文内容做问答。

直接浏览器打开访问网址：

## 一、第一步：设置用户名

首次打开页面，最上方有一个"用户名"输入框，**必须**先输入一个字符串后回车（或点别处失焦）保存，否则上传按钮会提示"请先在右上角输入用户名"。

用户名的作用：

- 你的对话历史按用户名存档；用同一个用户名换电脑、换浏览器都能找回
- 不同用户名的对话互不可见
- PPT 和检索索引按"PDF 内容 + 配置"全局共享，所以如果别人上传过同一篇论文+同一配置，你上传秒命中已生成的 PPT，但你看到的对话历史还是你自己的

用户名可以随便取（自己记得住就行），存在浏览器 localStorage 里。换浏览器只要输入同样的用户名就能看到自己的历史。

---

## 二、上传与生成

### 选择模式

中间有两个 tab：

- **单篇**：上传一个 PDF，生成单篇汇报 PPT
- **多篇综述**：上传 ≥ 2 个 PDF，生成综述风格的 PPT（按"研究背景 / 方法对比 / 综合结论"组织，不是简单并列）

### 拖文件 / 选文件

中间虚线框里：

- 拖拽 PDF 进去
- 或点击框内任意位置打开文件选择器

多篇模式可一次选多个文件。

### 配置 PPT

下方表单：

| 字段           | 选项                                                                        |
| -------------- | --------------------------------------------------------------------------- |
| **模板** | 学术答辩 / Anthropic / Google 风格 / 麦肯锡 / Exhibit / 重庆大学 / 自由设计 |
| **页数** | 8 / 10 / 12 / 15 / 20                                                       |
| **语言** | 中文 / 英文 / 中英双语                                                      |
| **风格** | 学术汇报 / 商务简报 / 技术分享                                              |
| **受众** | 高校师生 / 企业团队 / 通用                                                  |

点击底部「上传并生成 PPT」开始任务。

### 等待生成

跳到进度页：

- **PPT 生成**进度条 + 阶段提示（解析论文 → 模板选择 → 生成 SVG → 导出 PPTX）
- **知识库构建**进度条（PDF 解析 → 向量化 → 索引落盘）
- 底部黑色面板是实时滚动日志

两条任务并行跑。预期耗时（首次冷启动）：

- 单篇 ≈ 5–10 分钟
- 多篇综述 ≈ 8–15 分钟

完成后会**自动跳转**到阅读页。

> **缓存命中**：同样的 PDF + 同样的配置再次上传时，秒进阅读页，无需重新生成。

---

## 三、PPT 阅读 + 讲稿

```
┌─────────────────────────┬──────────────────────────┐
│  SVG 幻灯片预览          │  论文问答                  │
│                         │                          │
│  ┌─ 讲稿（可折叠）─┐    │   (右侧详见下一节)         │
│  │ 当前页的演讲稿   │    │                          │
│  └────────────┘       │                          │
│                         │                          │
│  [← 上页] [N/M] [→ 下页] │                          │
│  [▶ 连续播报] [下载]    │                          │
└─────────────────────────┴──────────────────────────┘
```

### 翻页

- 屏幕上 ← / → 按钮
- 键盘方向键 ← / →

### 讲稿

左下角"讲稿 ▼"点开看当前页的演讲稿文本。每页 100–200 字左右，是 AI 帮你写好的中文口语化稿子，可以直接照念。

### TTS 连续播报

点 "▶ 连续播报"：浏览器自带 TTS 从当前页开始朗读讲稿。读完一页自动翻页继续读，直到全部讲完或你点 "⏹ 停止播报"。

> 用 Chrome / Edge 浏览器效果最好。Safari 也支持但音色不同。

### 下载 PPTX

"下载" 按钮导出 `.pptx`，可在 PowerPoint / Keynote / WPS 里打开。

---

## 四、问答 + 引用高亮

右侧问答区：

```
┌────────────────────────────────────┐
│ 论文问答    新会话 ▾  [🔈]          │
├────────────────────────────────────┤
│                                    │
│  对话消息流                          │
│  AI 回答里 (第N页) 是蓝色按钮         │
│  点按钮 → 弹出 PDF + 黄色高亮         │
│                                    │
├────────────────────────────────────┤
│ [输入框]                [🎤] [发送]  │
└────────────────────────────────────┘
```

### 提问方式

- 输入框打字 + 回车发送（Shift+Enter 换行）
- 🎤 语音输入（需要 Chrome / Edge，会请求麦克风权限）

### 智能查询路由（多篇时）

当你在多篇综述空间里提问，系统会先判断你问的是哪几篇：

- "Chronos 是怎么处理时间的" → 只查 Chronos 那一篇
- "两篇论文分别有什么创新" → 两篇各查，确保都进入回答
- "实验结果如何" → 含糊问题，自动做综合检索

这一切自动发生，你只管自然提问就好。

### 答案里的引用

AI 回答里会在关键句末尾标 `(第N页)`，这是个蓝色按钮：

- **点击** → 右侧弹出 PDF 查看器
- **跳到对应页**
- **黄色高亮**那句话在原文中的位置

按 **ESC** 或点弹窗外区域关闭 PDF 查看器。

如果点了没看到高亮，可能是 AI 写的引用和原文有微小差异（标点、空格、特殊字符），后端会自动截到稳定可匹配的部分，高亮句子可能比答案里的引用短，但位置一定准确。

### TTS 自动朗读答案

右上角 🔈 切换成 🔊 后，每次新回答自动朗读。再点回 🔈 关掉。

---

## 五、多会话管理

每个空间下可以有多个独立的对话历史。同一空间，每个用户最多 **50 个会话**。

右上角下拉条：

```
论文问答   [当前会话 ▾]   🔈
              │
              ▼ 点开
        ┌──────────────────────┐
        │ 会话列表（3/50）  + 新建│
        │ ─ Chronos 时间处理 ·5  │
        │ ─ 实验对比 ·3          │
        │ ─ 新会话 ·1            │
        └──────────────────────┘
```

### 切换 / 新建 / 删除

- 点任一行切换，自动加载该会话历史消息
- "+ 新建"：起一个全新的空对话
- 每行右侧悬停出现 ✕：删除该会话（不影响其他）

### 自动起标题

新会话发出**第一条**消息后，系统会自动用 12 字以内的短语给会话起标题（比如 "Chronos 时间处理"）。下次再看列表时显示新标题。

### 进入空间时的默认会话

- 该用户在此空间有历史会话 → 自动加载**最近一次**会话继续聊
- 没有 → 自动建一个空会话

---

## 六、首页"我的历史空间"

上传区下方列出你的历史空间，每条卡片：

- 论文标题（多篇综述显示文件数量）
- 配置摘要：模板 · 页数 · 风格
- 状态徽章：
  - 🟢 **可阅读** — 已生成完成，点击进入
  - 🟡 **处理中** — 后台任务还在跑
  - 🔴 **已失败** — 任务报错（悬停看具体错误）
- ✕ 删除（只删你这边的元数据 + 对话历史；其他用户对该空间的访问不影响）

点击"可阅读"的卡片**直接进入空间**，不再走生成流程。

---

## 七、用户隔离与共享行为

| 资源                          | 跨用户共享 | 说明                   |
| ----------------------------- | ---------- | ---------------------- |
| PPT 产物（SVG / PPTX / 讲稿） | ✅         | PDF + 配置一致就秒命中 |
| 知识库索引（向量 + 关键词）   | ✅         | PDF 一致就复用         |
| 会话历史 / 消息               | ❌         | 按用户名隔离           |
| 我的历史空间列表              | ❌         | 只显示你访问过的       |

例：alice 和 bob 都用同一个配置上传 Chronos 论文 → 两人进同一个 `space_id`，alice 不会看到 bob 的对话，bob 也不会看到 alice 的对话，但底层 PPT 只生成了一份。

同样的论文换不同配置（比如换模板、换页数）→ 生成两份独立的 PPT，两个独立空间，你能在历史里都看到。

---

## 八、URL 语义

| URL                   | 含义                                          |
| --------------------- | --------------------------------------------- |
| `/`                 | 首页（用户名 + 上传 + 历史空间）              |
| `/tasks/<task_id>`  | 生成中进度页（短期，跑完跳走）                |
| `/space/<space_id>` | 知识空间阅读 + 问答页（**永久可分享**） |

`/space/<space_id>` 这个 URL 复制给别人，对方用自己的用户名打开就能进同一篇论文的阅读页，看到的 PPT/讲稿/PDF 都一样，但对话独立。

---

## 九、常见问题

**Q：上传后一直显示"处理中"，没动静？**

进度页底部黑底日志看最后一条。常见原因：网络慢、Claude CLI 卡住、LLM API 限流。多数情况下等几分钟会恢复。如果死了，回首页删掉这个空间，重新上传即可。

**Q：状态显示"已失败"，怎么办？**

鼠标悬停红色徽章看错误消息。常见：

- LLM API key 错或额度耗尽 → 联系管理员
- PDF 是扫描件没有文本层 → 换有文字的 PDF
- 网络中断 → 重试

点 ✕ 删除空间，重新上传同一份 PDF 同一配置即可重试。

**Q：点了引用按钮跳到 PDF 但没看见黄色高亮？**

按钮跳到的页数是对的，只是某些情况下原文短句和 AI 写的引用有不可见字符差异，导致没自动定位到具体句子。可以手动 Ctrl+F 在 PDF 里搜回答里那句话的关键词。

**Q：换电脑还能看到我的对话历史吗？**

可以。输入同样的用户名就行。所有数据都在服务器上，浏览器只保存你的用户名字符串。

**Q：旧的 `/session/...` 链接还能打开吗？**

会自动重定向。从"我的历史空间"重新进入对应论文即可（新的永久 URL 是 `/space/<space_id>`）。

**Q：怎么删除一个空间？**

首页历史列表里每条右边的 ✕。只删你这边的元数据和对话；其他用户的不受影响，底层 PPT/RAG 缓存也保留供下次访问。

**Q：会话能超过 50 个吗？**

到达上限后新建会失败，先删掉旧的不需要的会话再新建即可。
